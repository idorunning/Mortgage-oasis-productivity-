# Google credentials & cached token — NEVER commit these
credentials.json
token.json

# Local config (contains your folder IDs; copy from config.example.json)
config.json

# Client data stays local — regenerated by the refresh, not committed
data/tracker.json
data/statements.json
MortgageOasis-Dashboard.html

# Local environment & downloaded-workbook cache
.venv/
.cache/
__pycache__/
*.pyc
